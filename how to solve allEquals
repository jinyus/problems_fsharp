allEqual [1,2,3,4] -> false
allEqual [1,1,1,1] -> false

Testing if all elements in a list are equal

1: list.min == list.max

2: Check if every element == first element

3: Get unique elements and checking if number of uniques == 1

4: Check if every 2 adjacent elements are = to each other